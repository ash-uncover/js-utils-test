{
  "name": "ap-utils-test",
  "version": "0.1.0",
  "description": "javascript test utils",
  "main": "dist/index.js",
  "types": "dist/index.js",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm test && npm run lint",
    "preversion": "npm run lint",
    "test": "jest",
    "test-coverage": "jest --collectCoverage"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ash-uncover/ap-utils-test"
  },
  "author": "aSH <ash.uncover@gmail.com>",
  "license": "ISC",
  "peerDependencies": {},
  "devDependencies": {
    "@types/jest": "latest",
    "@types/node": "latest",
    "jest": "latest",
    "prettier": "latest",
    "ts-jest": "latest",
    "tslint": "latest",
    "tslint-config-prettier": "latest",
    "typescript": "latest"
  },
  "dependencies": {}
}
